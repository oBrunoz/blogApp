<div class="main">
  <!-- Header -->
<header style="padding: 0; width: 100%; height: 100%;" class="bg-primary text-white text-center py-5">
  <div class="container">
    <div class="row">
      <div class="col-lg-7 mx-auto">
        <h1 class="display-4 font-weight-bold">Seja bem-vindo!</h1>
        <p class="lead">Espero que você goste desse projeto de blog. <b>Crie, edite e exclua</b> suas postagens quando quiser, e fique por dentro de postagens de outros usuários!</p>
        <a href="/users/registro" class="btn btn-light btn-lg mt-3">Criar uma conta</a>
      </div>
    </div>
    {{!-- <div class="row">
      <div class="col-lg-10 mx-auto mt-5">
        <form class="form-inline d-flex justify-content-center">
          <input type="email" class="form-control mr-3" placeholder="Seu endereço de e-mail">
          <button type="submit" class="btn btn-success">Inscreva-se</button>
        </form>
      </div>
    </div> --}}
  </div>
</header>

<hr>

{{#each postagens}}
<ul class="list-group">
  <li class="list-group-item">
    <h5 class="mb-1 text-primary">{{titulo}}</h5>
    <p class="mb-1">{{desc}}</p>
    <a href="/posts/{{slug}}"><button type="button" style="float: right; margin: 5%; position: relative;" class="btn btn-outline-success btn-block my-4">Leia mais</button></a>
    <hr>
    <small >Data de publicação: {{date}}</small>
  </li>
</ul>

{{else}}
    <h5 class="">Nenhuma postagem!</h5>

{{/each}}

</div>