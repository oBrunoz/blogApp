<h1>Lista de Formulários:</h1>

    {{#each posts}}

        <small>{{dataValues.createdAt}}</small>
        <h1>{{dataValues.titulo}}</h1>
        <p>{{dataValues.conteudo}}</p>
        <a href="/delet/{{dataValues.id}}"><button>Deletar</button></a>
        <hr>

    {{/each}}
